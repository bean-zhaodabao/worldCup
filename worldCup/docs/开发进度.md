# 开发进度记录

> 本文档记录数据接入（vipc.cn 竞彩足球接口）的实时进度，每次大改动更新。部署步骤详见《数据接入部署说明.md》。

## 当前状态：已部署验证 ✅（2026-08-20）

核心链路已上线并实测通过：sync-jczq 每分钟定时同步赛事/玩法/盘口 → 管理端展示与人工覆盖 → 移动端展示与下注 → 完场自动结算。

## 已完成功能（2026-08-20）

### 数据同步（sync-jczq 云函数）
- 赛事列表 upsert（sourceMatchId 幂等，状态单调推进，比分/半场比分/加时点球）
- 未开赛赛事拉取竞彩官方赔率（胜平负/让球胜平负/比分31项/总进球区间/半全场9项）+ 盘口（亚盘/大小球）
- 盘口只取 companyId 8/12/14 三家公司合并：**同盘口去重、每侧取最低水位**（`odds-core.js mergePankou`）
- 水位为香港盘 → 内部存十进制赔率（1+水位）
- 玩法 upsert（sourceKey 幂等），赔率变化 oddsVersion++（移动端 2s 轮询刷新）
- 自动结算引擎：完场按 90 分钟比分判定（走盘退本金/半赢半输/四分盘拆半）、串关全腿完场后结算、取消场次整单退款、中奖自动入账

### 单元测试
- `sync-jczq/test-odds-core.js`：**71 项全部通过**（盘口解析/合并、区间赔率 1/Σ(1/oi)、结算判定）

### 管理端
- 赛事：立即同步（显示统计+错误）、上下架、状态推进、删除
- 玩法：「盘口/水位」列显示中文盘口+水位（如 `受让半球/一球　0.55`），原始/展示双赔率、人工覆盖、恢复跟随、停售
- 用户（类型/充值）、提现审核、订单（refunded 状态）

### 移动端
- 首页日期分组 + SPF 简览；详情页 5 大类 Tab + 盘口表格（中文盘口列）
- 单关（全部玩法均可）、串关 2-8 场、下单服务端重读实时赔率、过期拒单
- 钱包/流水/提现（申请冻结→审核通过扣减/驳回解冻）；收单账户不校验余额

## 2026-08-20 上线调试修复

| # | 问题 | 修复 | 状态 |
|---|---|---|---|
| 1 | admin-match 缺 `const path = event.path || ''` → "path is not defined"（同步/上架/删除/状态全挂） | 补上变量定义 | ✅ 已修已部署 |
| 2 | 同步按钮看不到内部统计/错误 | 弹窗显示 赛事/玩法新增更新数、结算数、前3条错误 | ✅ 已修（build 管理端生效） |
| 3 | 玩法页「盘口/水位」只显示水位数字，看不出盘口 | 改为中文盘口+水位，与移动端一致 | ✅ 已修（build 管理端生效） |
| 4 | 移动端玩法被截断（uniCloud get 默认 100 条上限，全场只剩 9 个玩法） | user-match 玩法查询改 skip/limit(1000) 分页取全 | ✅ 已改代码，**待重新上传 user-match** |
| 5 | 单元测试 2 项因断言漏 `receive` 字段失败（合并逻辑无误） | 补断言字段 | ✅ 71 项全过 |
| 6 | 盘口三家公司合并只需即时赔率、不要初始赔率 | 核实：mergePankou 只用 `odds`（即时），firstOdds/firstPankou 从未使用 | ✅ 已满足（无代码改动） |

## 线上实测数据（2026-08-20）

- 手动同步：耗时 2.4s，errors 空；赛事更新 32、玩法新增 20/更新 1116、赔率变化 12 场
- 管理端盘口玩法齐全，赔率与 vipc 接口实时一致
- 定时器每分钟正常触发

## 待办

1. **重新上传 user-match 云函数**，移动端确认单场完整显示 55 个玩法（胜平负3 + 让球3 + 总进球3 + 比分31 + 半全场9 + 让球盘 + 大小球）
2. 管理端重新 build 并部署（dist 已生成于 `worldCup/admin/dist`），生效「同步统计提示 + 中文盘口列」
3. 待用户决定：竞彩官方玩法（胜平负/让球胜平负）的初始赔率 oddsFirst/oddsStat 存储是否删除（当前存储但无页面展示）
4. 接口 live 状态取值未实测到（样本只有 0/-1），status>0=live 的映射待观察
5. admin-order 风控统计查询（当日订单/中奖总额）无 limit，量大后会截断统计，当前量小可接受
6. sync-jczq 云函数超时需在 uniCloud 控制台设为 60s（详见部署说明）

## 规则基线

- 移动端只展示未开赛赛事；赛事自动上架，管理端可手动上下架
- stop=true 自动停售不可投注；下单以服务端实时赔率为准
- 中奖入账 = 下注额 × 赔率（含本金）；散户下单即扣款，收单账户允许负余额
- 串关 2-8 场、同场不可重复；含取消场次整单退本金
